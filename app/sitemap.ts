import type { MetadataRoute } from "next";import { getPublishedIssues } from "@/lib/content";export const revalidate=0;export default async function sitemap():Promise<MetadataRoute.Sitemap>{const base=process.env.NEXT_PUBLIC_SITE_URL||"http://localhost:3000";const issues=await getPublishedIssues();return [{url:base},{url:`${base}/issues`},...issues.map(i=>({url:`${base}/issues/${i.issue_id}`,lastModified:new Date(i.publish_date)}))]}
